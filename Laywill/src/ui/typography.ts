import { theme } from "./theme";

export const H1 = { fontSize: 24, fontWeight: "800" as const, color: theme.colors.text };
export const H2 = { fontSize: 18, fontWeight: "800" as const, color: theme.colors.text };
export const Muted = { fontSize: 12, color: theme.colors.muted };
export const Money = { fontSize: 30, fontWeight: "900" as const, color: theme.colors.text };
